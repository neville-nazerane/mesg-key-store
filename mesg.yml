name: "Key Store"
sid: "key-store"
description: "Easy to use storage for keys"
tasks:
  fetch:
    inputs:
      alias:
        type: string
    outputs:
      key:
        type: String
configuration:
  ports:
    - "5321"
