name: "Key Store"
sid: "key-store"
description: "Easy to use storage for keys"
tasks:
  fetch:
    inputs:
      alias:
        type: String
    outputs:
      success:
        data:
          key:
            type: String
configuration:
  ports:
    - "5321"
